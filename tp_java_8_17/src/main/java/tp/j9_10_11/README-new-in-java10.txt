* var keyword (type inference)

* new java version every 6months

* Unmodifiable collection improvement:
 Collections.unmodifiableList(...);     .collect(Collectors.toUnmodifiableList());
 
 *internal JVM improvements
  
* .... 
